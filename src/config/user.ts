// Disclaimer: This user info is mocked. This info should be obtained after logging into the application 
// as a response from a login request.

import { UserProps } from "../@types";

export const userInfo: UserProps = {
  associationCode: 601,
  userEmail: "romulo.marques@hinovamobile.com.br",
};
